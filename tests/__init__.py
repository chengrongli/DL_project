"""
Unit tests for the LPC sprite diffusion project.

Tests are intentionally lightweight (no GPU required, no large data files)
so they can be run on any machine with PyTorch installed.

Run:
    python -m pytest tests/ -v
"""
